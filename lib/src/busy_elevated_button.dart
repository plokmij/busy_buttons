import 'package:flutter/material.dart';

class BusyElevatedButton extends StatelessWidget {
  const BusyElevatedButton({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
