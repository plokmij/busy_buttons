import 'package:flutter/material.dart';

class BusyIconButton extends StatelessWidget {
  const BusyIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
